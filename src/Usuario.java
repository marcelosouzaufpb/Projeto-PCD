import java.io.Serializable;

public abstract class Usuario implements Serializable {
	/*
	 * Essa classe vai definir o que o servidor vai receber, sendo um cliente ou um
	 * trabalhador ou uma lista de usuários(usuários são clientes ou trabalhadores)
	 * 
	 */
}
